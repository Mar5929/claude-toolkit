# Review 6, round three: is the design ready for the owner to read

Document reviewed: `/home/user/claude-toolkit/docs/designs/269-knowledge-system.md`, 2,865 lines, read in full on 2026-09-16.
Rulings checked: `fix-list-round-2.md` C1 to C10 and D1 to D7, against `fix-report-round-2.md`.
Blockers re-checked: review 4 findings 1 and 2, review 5 findings F1, F2, F3.
Sources opened for spot checks: `knowledge/prds/knowledge-system.md`; `hooks.md`, `skills.md`, `sub-agents.md`, `plugins-reference.md` under `ai-external-knowledge/claude-code/`; `plugins/second-brain/` on disk; the four repository checks were run.

Counts: 17 rulings landed, 1 landed in part (C9), 0 not landed. All five round-two blockers are gone. New findings: 0 blockers, 10 should-fix, 7 nits.

## Section 1. Ruling and blocker status

| Id | Status | Location |
| --- | --- | --- |
| C1, `startup-state.mjs` fits 9,500 | Landed | 6.4 lines 1013 to 1025 (100 + 1,200 + 1,500 + 200 + 100 + 5,000 + 200 = 8,300, recomputed and correct); parts table rows 182, 183, 184, 187; 13.14 line 2412; 13.22 line 2539; question 26. No occurrence of a 2,000-character glossary remains |
| C2, `hooks.json` in item 1 | Landed | Item 1 Files row 2088 creates `hooks/hooks.json` with both `SessionStart` entries and removes this repository's old `SessionStart` entry and hook copy; lines 2092 to 2101 say what item 1 can test alone; item 2 note 2110 adds the remaining entries |
| C3, measured standing-rule size | Landed | 6.2 lines 640 to 643: 1,998 characters, 26 lines, replacing a 2,438-character draft. The number appears nine times, including parts table row 191, cost table 2035, 9.3 row 1797 and question 14 |
| C4, merge the duplicate questions | Landed | Section 15 holds 27 questions numbered 1 to 27; the nudge is question 2 only (2729 to 2739); the walkthrough edit is question 5 only (2747 to 2751); intro count at 2722 says twenty-seven |
| C5, a recommendation on every question | Landed | Questions 2 (2735), 5 (2750), 15 (2785), 19 (2806), 22 (2816) each carry a bolded recommendation |
| C6, fix the pre-commit cross-reference | Landed | 6.5 line 1416 now points at open question 16 only, which is the pre-commit question. Every "see 13.x" and "question N" reference resolves; see Section 2, finding 6, for the one gap this sweep leaves |
| C7, cost, effort and rollback | Landed | Section 12 lines 2026 to 2071: a before-and-after cost table in characters and tokens, an eight-row effort estimate marked as an estimate, and a rollback paragraph |
| C8, keep-or-refactor verdict | Landed | Section 9 lines 1694 to 1707, with the 20,585-character measurement behind it |
| C9, trim restatements | Landed in part | Five section 5 rows, three section 14 recommended blocks and three duplicate paragraphs were cut; the document still grew from 2,671 to 2,865 lines. Reported by the fixer with its reason. See Section 2, finding 10 |
| C10, remaining review 5 items | Landed | Spot-checked F4 (238 to 245), F14 (453 to 456), F19 (934 to 940), F20 (994 to 998), F21 (182), F22 (1389 to 1399), F23 (2090), F24 (2563 to 2574), the seventeen new term rows (25 to 57) and the ten rewritten sentences |
| D1, index rows in the parts table | Landed | Lines 183 and 184: "Path and entry count printed at start; contents opened during a lookup", "About 60 characters at start" |
| D2, 13.13 Codex gaps | Landed | 13.13 lines 2385 to 2399 names the compaction hold, the missing `if`, `once` and `args`, and the trust rule |
| D3, `compact-hold.mjs` timeout | Landed | 5 seconds at lines 229 to 231, 898 to 899, and the section 10 timeout test at 1914 |
| D4, one line count for the rule | Landed | 26 lines at 191, 632, 640, 644, 1548, 1797 and 2637 |
| D5, last research-note citation | Landed | 9.4 lines 1809 to 1811 reads "the inventory in the research notes for this design, 2026-09-16, not in the repository" |
| D6, PRD line numbers | Landed | 1469 at 1772, 1773, 2529, 2831; 1075 at 474 and 2541; 1308 at 2494, 2497, 2506, 2570, 2825. All five verified against the PRD file |
| D7, remaining review 4 items | Landed | Spot-checked findings 6 (1001 to 1006), 7 (775 to 777), 9 (2083), 10 (459 and 2499), 13 (glossary rows 56 and 198), 15 (rows 178 to 181) |
| Review 4 blocker 1, `startup-state.mjs` worst case | Gone | 6.4 lines 1013 to 1020 state and sum the real number, 8,300 against 9,500; the section 10 seeded test at 1900 asks for 7,600 and 8,300 |
| Review 4 blocker 2, stale index rows | Gone | Parts table lines 183 and 184 |
| Review 5 blocker F1, same arithmetic | Gone | As above. `current.md` is no longer the first thing dropped in the worst case, because the glossary and inbox caps make the sum fit |
| Review 5 blocker F2, item 1 not buildable | Gone | Item 1 creates the registration file, removes the old registration, and lists what it can test on its own |
| Review 5 blocker F3, standing-rule cost | Gone | 1,998 characters, measured, in all nine places |

Arithmetic recomputed from the numbers in the text, all correct: `startup-files.mjs` 100 + 1,000 + 1,500 + 5,000 = 7,600; `startup-state.mjs` 100 + 1,200 + 1,500 + 200 + 100 + 5,000 + 200 = 8,300; the two together 15,900 against a 19,000 allowance; per request 1,998 + under 2,000 = about 4,000 characters, about 1,000 tokens; 400 requests a day, about 400,000 tokens; four session starts 10,000 to 16,000 tokens; the manual outline's ten budgets total 3,250. Section 4, 6.4, section 12 and questions 14 and 26 agree on every one of these.

Counts recounted against the text: 7 hooks, 4 skills, 6 tool and Git-hook entries in 6.5, 30 requirement rows in section 7, 26 assumption tests in section 10, 12 Codex proofs in 8.6, 12 migration steps in 9.5, 23 entries in section 13, 8 options in section 14, 27 questions in section 15, 8 build items plus step zero, 5 repository checks in 11.4, 10 manual outline parts. Every stated count matches except the one in finding 7.

Facts checked against primary sources, all correct: `knowledge/README.md` is 13,395 characters; `SOUL.md` is 443; `knowledge/current.md` is 1,847; the four over-long `summary` values measure 225, 241, 229 and 211; `check-knowledge.mjs` holds `CURRENT_MD_MAX_CHARS = 2000`, `SUMMARY_MAX_CHARS = 250` and the SHA-256 pin at line 32 with the hash the design quotes; `plugins/second-brain/hooks/` holds five files and `skills/` holds six folders; `plugin.json` carries a `version` field; `ai-external-knowledge/claude-code/` holds 161 captured pages and its README says 161. In the captured documentation, `hooks.md` lines 431, 844, 848, 916, 996, 126, 435, 376, 1365, 993, 2477 and 2989 say what the design says they say, including the 10,000-character output cap and the 8-consecutive-continuation cap; `skills.md` gives the 1,536-character cap and the 5,000-and-25,000-token re-attachment budget; `plugins-reference.md` line 905 says a plugin's CLAUDE.md is not loaded as project context. All four repository checks pass, the two code fences are balanced, and both hand edits named in the fix list survive.

## Section 2. Remaining findings

| id | severity | location | problem | fix |
| --- | --- | --- | --- | --- |
| 1 | should-fix | Question 15, lines 2784 to 2789, against 6.1 lines 453 to 456 | The recommendation raises `knowledge/project.md` guidance to 2,000 and lowers the manual's guidance to 4,500, then says "the manual's 4,000-character warning and 5,000-character ceiling in the checker stay as they are". A manual written to the recommended 4,500 trips the checker's warning on its first save. Mike is being asked to approve a guidance number the design's own checker complains about | Recommend 4,000 for the manual, which is already 6.1's target, and give `knowledge/project.md` the freed 500: 100 + 1,000 + 2,000 + 4,000 is 7,100, still inside 9,500. Or raise the checker's warning to 4,500 in the same breath and say so |
| 2 | should-fix | 9.3 row at line 1789 and 13.10 lines 2340 to 2348, against 6.1 lines 550 to 555 | Both say `ai-external-knowledge/README.md` is hand-written today by this repository's capture script. 6.1 says it does not exist, and it does not: the folder holds only `claude-code/`, whose own `README.md` is the hand-written topic index. The two places also label the work "Change" rather than "Add", which tells the builder to convert a file that is not there | In 9.3 make the row "Add: generated by the builder from each topic's entry page. The root file does not exist today; the hand-written index is the topic index at `ai-external-knowledge/claude-code/README.md`". Make the same correction in 13.10 |
| 3 | should-fix | 6.4 lines 1108 to 1110, and 13.8 lines 2317 to 2319 | "because the guard set the owner named on 2026-09-03 ... covers `gh pr create`, `work finish`, and `gh issue close` and excludes merge". A reader parses "the guard set the owner" as subject and verb and has to start again. Nothing else in the design calls the three commands "the guard set" | "Merging a pull request raises no hold. On 2026-09-03 the owner named the commands the gate holds, recorded in issue 269's comment of that date: `gh pr create`, `work finish`, and `gh issue close`. Merge is not one of them." Use the same sentence in both places |
| 4 | should-fix | 13.11 heading line 2354; 9.1 per-Bash row line 1737; 14.3 line 2630; 9.3 link-check row line 1793 | Four PRD citations point at the wrong line. Function hooks are named at PRD lines 1826 to 1831, not 1938 to 1942, which hold the "Explore these questions" bullets. "Do not watch or control every action just because it is possible" is PRD line 1699, not 1711. "Add more restrictions only when a failure that actually happened calls for them" is PRD line 1698, not 1684, which is the lightest-check line. Requirement 1's check on links and the index is PRD lines 466 to 470; line 465 is blank | Change 1938 to 1942 to 1826 to 1831; 1711 to 1699; 1684 to 1698; 465 to 466. Requirement 26 asks the design to be checkable against its sources, and these four are the only citations in the document that do not land |
| 5 | should-fix | 6.2 lines 688 to 696 | The design cites `sub-agents.md` lines 1036 and 1041 for what a subagent loads, then says a subagent can carry `omitClaudeMd`. Line 1041 is the sentence "Explore and Plan are the only subagents that omit CLAUDE.md and git status. There is no frontmatter field or per-agent setting to change which agents..." The design notes the field is not in the capture, but not that the capture actively denies it. `.claude/rules/claude-code-docs-first.md` says to name a disagreement out loud rather than pick one quietly | Add one sentence: "The captured page says the opposite at line 1041: it states there is no frontmatter field for this. The field is recorded in the research notes for this design, 2026-09-16, and step zero's refresh settles which is current." Also say "project rules" is an inference from the CLAUDE.md hierarchy line, or drop the phrase |
| 6 | should-fix | Section 13 entries 13.9 (2328), 13.10 (2340), 13.11 (2354), against section 1 line 12 | Section 1 tells Mike "the second half of this document lists every open question for the owner to answer". Three section 13 entries have a recommended answer and an "if Mike answers differently" clause but no question in section 15 and no reference anywhere else, so answering all 27 questions leaves them unanswered | Either add three questions (Node scripts as part of a hook or skill; the generated outside-documentation index; building on command hooks rather than waiting for function hooks), or say at the head of section 13 that 13.9, 13.10 and 13.11 are recorded decisions that need no answer unless Mike disagrees |
| 7 | should-fix | Section 4, line 213 | "Three notes on the table" is followed by four paragraphs: the rule file, the double registration, the timeouts, and the always-on cost | Say "Four notes on the table", or give the always-on cost its own short heading, since it is the number section 12 refers back to |
| 8 | should-fix | Question 14, lines 2776 to 2779 | "the per-part guidance is `SOUL.md` under 1,000, `knowledge/project.md` under 1,500, and the manual under 5,000, which totals 7,600". Those three total 7,500. The missing 100 is the version line, which the sentence does not name. Mike is being asked whether the numbers are right, so the sum has to add up on the page | "...the version line at 100, `SOUL.md` under 1,000, `knowledge/project.md` under 1,500, and the manual under 5,000, which totals 7,600." |
| 9 | should-fix | Section 4 row 206, and 6.5 line 1391 | Both say `command-parsing.mjs` is "kept from today's `save-reminder.mjs`", which reads as an extraction from that hook. It is already its own shipped module at `plugins/second-brain/hooks/command-parsing.mjs`, which is what 9.1 line 1733 says | "The shared shell-command parser, kept unchanged. It ships today at `plugins/second-brain/hooks/command-parsing.mjs` and `save-reminder.mjs` imports it" |
| 10 | should-fix | Whole document, 2,865 lines | Ruling C9 asked for a trim and the document grew by 194 lines. The fixer explains why and the added material was all asked for, so this is not a fault in the content. It is still the thing most likely to stop Mike finishing it: there is no place he can read in five minutes and act | Add a one-page block after section 1: the verdict sentence from section 9, the always-on and per-start cost in tokens, the eight build items with their effort estimates, and the 27 questions as a numbered list of one line each with the recommendation in bold. Nothing new is written; it points at what is already there |
| 11 | nit | 6.1 line 435 | "Today's copy is 13,395 characters, which is 65 percent of the startup output that overflows the 10,000-character cap." A reader can take 65 percent to mean 65 percent of the overflow rather than of the whole output | "Today's copy is 13,395 characters, which is 65 percent of the 20,585 characters the startup hook prints." |
| 12 | nit | Move, shrink or go table, line 2574 | "It reads as heavy" is a picture word, and review 5 asked for the same word to be removed elsewhere | "It is a lot of text, and it is what lets the owner answer a card a day later without being shown it again." |
| 13 | nit | Section 1, line 22 | "Where this design follows the walkthrough against the requirements document, it says so on the line." A junior asks which line | "Where this design follows the walkthrough against the requirements document, it says so where it does." |
| 14 | nit | 6.1 line 525 | "so a long entry costs nothing at startup" is not true: a long entry costs the same 60 characters as a short one | "so a long entry costs no more at startup than a short one." |
| 15 | nit | Section 9 line 1706, and section 12 line 2065 | "Nothing in the store has to be thrown away" uses "the store" for the knowledge files, which the design never defines that way. "provided it happens before a later save lands on top of them" is figurative | "No knowledge file has to be deleted." And "provided no later save has been committed over them; after that the revert is a merge the owner resolves." |
| 16 | nit | 6.4 line 976 | "The cheap, high-value items come first" uses "cheap" for "short" | "The short items that matter most come first, so the overflow rule never reaches them." |
| 17 | nit | Flowchart line 363, and the closing heading at line 2841 | `I -- No --> H` sends "not a save moment" back into "Do the work", so the diagram has a loop with no end state and no node for the session ending. Separately, "Alternatives considered for the whole system" is a second level-one heading outside the section numbering, so section 9 can only call it "the closing table" | Add a terminal node, for example `I -- No --> END[Turn ends]`. Number the closing section 16 so it can be referenced |

Plain-English sweep, collected: findings 3, 7, 8, 11, 12, 13, 14, 15 and 16 are the sentences a junior reader would misread or the figurative words still in the text. Ten sentences, each with its rewrite above. Nothing else in the document uses a picture word for a real thing, every term in section 4 onward is defined in the section 1 table or at first use, and every heading names what sits under it.

Statements checked against the PRD and the captured documentation and found true: the 10,000-character hook output cap and its spill behaviour; the 8-consecutive-continuation cap on `Stop`; the one-rule `if` field; the plugin-scoped MCP tool name; `UserPromptExpansion` as the typed-skill path; `PreCompact` blocking with exit 2 and the stderr message going to the user; the 1,536-character skill description cap; the 5,000-and-25,000-token skill re-attachment budget; a plugin not being able to ship a rules file; requirement 14's twelve fields; requirement 16's eight requirements-document fields; requirement 18's line 1308; requirement 21's two size limits; requirement 29's refusal of a scorer. The only statements that do not hold are findings 2, 4 and 5.

## Section 3. Verdict

**Ready for the owner, after the four edits below.** No blocker remains. Every round-two ruling landed, C9 in part and reported. The five blockers from reviews 4 and 5 are gone, and the two arithmetic errors that caused them recompute correctly from the numbers now in the text. Every count the document states matches the document, every internal cross-reference resolves, and every claim I could check against the PRD, the captured pages or the files on disk is true except the three named above.

The shortest list of edits that would make it ready:

1. **Finding 1.** Make question 15's recommended manual size agree with the checker's 4,000-character warning. This is the one place where Mike is asked to approve a number that the design elsewhere refuses.
2. **Finding 2.** Correct the two places that say `ai-external-knowledge/README.md` exists today. It does not, and the row tells the builder to convert a missing file.
3. **Findings 4 and 5.** Fix the four wrong PRD line numbers, and say out loud that the captured `sub-agents.md` denies the `omitClaudeMd` field the design relies on.
4. **Finding 10.** Add the one-page decision block after section 1. Nothing in it is new writing; it is the fastest way to make 2,865 lines answerable in one sitting.

Findings 3 and 6 to 9 are each a one-line edit and can travel with the four above. The eight nits change no fact and can wait for the next pass.

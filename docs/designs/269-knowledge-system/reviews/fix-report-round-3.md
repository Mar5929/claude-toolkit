# Fix report, round three

File edited: `docs/designs/269-knowledge-system.md`. No other file changed, nothing committed.
Line count: 2,865 before, 2,944 after. The 79 added lines are the 60-line "Decisions at a glance"
section plus the longer replacement text in 6.2, 6.5, 13.8 and 13.10.

## Section 2 findings, all applied

| id | What was changed |
| --- | --- |
| 1 | Question 15 now recommends raising `knowledge/project.md` to 2,000 and holding the manual at its 4,000-character target. Its arithmetic reads 100 + 1,000 + 2,000 + 4,000 = 7,100, inside 9,500. The checker's 4,000 warning and 5,000 ceiling are stated unchanged. No "4,500" remains in the document |
| 2 | The 9.3 row is now "Add: generated by the builder from each topic's entry page", with "The root file does not exist today; the hand-written index is the topic index at `ai-external-knowledge/claude-code/README.md`". 13.10's opening paragraph says the same, and its "If Mike answers differently" clause was reworded from "if the file stays hand-written" to "if the root index is written by hand instead" |
| 3 | Both 6.4 and 13.8 now read "Merging a pull request raises no hold. On 2026-09-03 the owner named the commands the gate holds, recorded in issue 269's comment of that date: `gh pr create`, `work finish`, and `gh issue close`. Merge is not one of them." |
| 4 | Four PRD citations corrected: 13.11 heading 1938 to 1942 becomes 1826 to 1831; 9.1's per-Bash row 1711 becomes 1699; 14.3's 1684 becomes 1698; 9.3's link-check row 465 becomes 466. Each target line was read in `knowledge/prds/knowledge-system.md` before the change |
| 5 | 6.2 now cites line 1036 alone for the CLAUDE.md hierarchy, says the "project rules" part is this design's inference from that line, and adds a paragraph naming the disagreement: the captured page denies the field at line 1041 (quoted), the capture is dated 2026-09-04, the live documentation read 2026-09-16 lists it, and step zero's refresh settles which is current, so it is a build-time check |
| 6 | Section 13's introduction gained: "Three of the entries, 13.9, 13.10 and 13.11, are recorded decisions rather than questions, which is why they have no entry in section 15. The design proceeds on the recommended answer unless Mike says otherwise." The alternative, adding three questions, was not used, so section 15 still holds 27 |
| 7 | "Three notes on the table" becomes "Four notes on the table" |
| 8 | Question 14 now names the version line at 100, so the sum reaches 7,600, and adds that the manual's target is 4,000 while 5,000 is the checker's ceiling, which this budget uses |
| 9 | Section 4's row and 6.5 both say the parser ships today at `plugins/second-brain/hooks/command-parsing.mjs`, where `save-reminder.mjs` imports it, and that `save-moment-gate.mjs` imports it after this design lands |
| 10 | New "## 1a. Decisions at a glance", 60 lines, between section 1 and section 2 |
| 11 | "65 percent of the 20,585 characters the startup hook prints" |
| 12 | "It reads as heavy" becomes "It is a lot of text" |
| 13 | "says so on the line" becomes "says so where it does" |
| 14 | "costs nothing at startup" becomes "costs no more at startup than a short one" |
| 15 | Section 9's verdict becomes "No knowledge file has to be deleted", and section 12's rollback becomes "provided no later save has been committed over them; after that the revert is a merge the owner resolves" |
| 16 | "The cheap, high-value items come first" becomes "The short items that matter most come first" |
| 17 | Flowchart edge `I -- No --> H` becomes `I -- No --> END[Turn ends]`. The closing heading is now `## 16. Alternatives considered for the whole system`, and section 9's "the closing table says why" becomes "section 16 says why" |

## Section 3 edits, all applied

1. Ruling 1 followed. 4,000 is the target and 5,000 the hard fail in every place. The
   `startup-files.mjs` arithmetic still budgets 5,000 for the manual, and question 14 now says
   why.
2. Ruling 2 followed. 9.3 and 13.10 match 6.1: the file does not exist and the design adds it.
3. Ruling 3 followed. Four citations fixed; 6.2 names the disagreement and cites captured line
   1041.
4. Ruling 4 followed. The block is numbered 1a, so sections 2 to 16 keep their numbers. It holds
   the design in five lines, a seven-row table of part kinds with counts, the five enforced
   things and the statement that everything else is guidance, six lines on what changes, the
   eight decisions with their recommendation and section 15 question number, and one paragraph
   of section pointers.

## Declined

Nothing was declined. Finding 6 had two options and the second, a note at the head of section 13,
was taken, because adding three questions would have changed the question count in section 1,
section 15's introduction and the new 1a block.

## Checks

- `node tests/link-check.mjs`: ALL PASS, 135 relative links across 187 Markdown files
- `node tests/orphan-check.mjs`: ALL PASS, 148 shipped files reachable from 59 index documents
- `node tests/installed-copy-check.mjs`: ALL PASS, 21 checks
- `node tests/knowledge-startup-check.mjs`: ALL PASS, 37 checks
- Section 7: 30 requirement rows
- Section 15: introduction says "Twenty-seven questions", and 27 numbered questions follow
- Code fences: 4 markers, balanced
- Constraints held: the five old card labels appear in backticks only, never bolded with a colon;
  `knowledge-policy` is never followed by a colon character; no `/tmp` path appears
- `git status`: `docs/designs/269-knowledge-system.md` is the only modified file

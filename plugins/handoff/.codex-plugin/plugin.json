{
  "name": "handoff",
  "description": "Ends a long session without losing what it learned. Runs the project's memory check first, shows what it proposes to save as a table the owner approves, cuts, or edits, and only then writes a self-contained prompt for a fresh session. Anything not saved to memory is carried inside that prompt instead of being dropped.",
  "version": "0.1.0",
  "author": {
    "name": "Mike Rihm",
    "email": "michael@rihm.com"
  }
}

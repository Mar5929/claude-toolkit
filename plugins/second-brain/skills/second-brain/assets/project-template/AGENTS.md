# Codex project adapter

Read `PROJECT.md` as the canonical project router. Use the paths and authorities
declared there and in `memory/config.yaml`. Do not duplicate project knowledge
in this adapter.

# Archived v1 infrastructure record

Second-brain v1 is retired. Do not create, rebuild, synchronize, or deploy its
Cloudflare Worker, OAuth application, KV namespace, Workers AI binding, or Neon
databases.

The former setup procedure remains available in Git history for audit purposes.
It is intentionally absent from the shipped plugin so it cannot be mistaken for
an approved infrastructure path.

Existing cloud resources remain untouched until the owner separately approves
deletion.

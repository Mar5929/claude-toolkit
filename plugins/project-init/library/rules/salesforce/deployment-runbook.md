---
paths:
  - "delivery/deployment/**"
  - "engagement/deployment/**"
---
# Deployment Runbook

- When metadata needs an action the deploy cannot perform, add a step to `manual-steps.md` in the work item's manifest folder, in the same response.
- Open the `sf-component-tracker` skill (`.claude/skills/sf-component-tracker/SKILL.md`) for the step shape and the trigger list.
- When unsure, add the step.
